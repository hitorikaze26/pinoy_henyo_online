Flask>=3.0
Flask-CORS>=4.0
Flask-SQLAlchemy>=3.1
Flask-Migrate>=4.0
Flask-SocketIO>=5.3
# eventlet>=0.33  # UNCOMMENT for Render (gunicorn -k eventlet worker).
#                 # Leave commented for local dev so socketio.run stays on the
#                 # default 'threading' async mode.
PyMySQL>=1.1
simple-websocket>=1.0
python-dotenv>=1.0
pytest>=8.0
segno>=1.6